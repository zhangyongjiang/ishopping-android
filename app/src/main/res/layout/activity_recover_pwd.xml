<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/signin_bg"
    tools:context="com.nextshopper.activity.RecoverPwdActivity">
    <com.nextshopper.view.TitleView
        android:id="@+id/recover_pwd_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:textMiddle="@string/recover_pwd"
        app:imageLeft="@drawable/back"
        app:textRight="@string/send"
        />
    <TextView
        android:id="@+id/recover_pwd_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:textColor="@color/white"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/recover_pwd_title"
        android:text="@string/recover_pwd_text"/>

    <EditText
        android:id="@+id/recover_pwd_email"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="20dp"
        android:layout_below="@id/recover_pwd_text"
        android:hint="@string/email"
        android:textSize="20sp"
        android:paddingLeft="10dp"
        android:background="@drawable/rectangle_input"
        />
</RelativeLayout>
